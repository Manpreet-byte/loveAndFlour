# Frontend environment variables (Vite)

# Backend base URL (optional). If empty, Vite proxy can be used.
# VITE_API_BASE_URL=http://localhost:8081

# Web Push (VAPID public key)
VITE_VAPID_PUBLIC_KEY=

